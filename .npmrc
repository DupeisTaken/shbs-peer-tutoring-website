# Auth.js beta peer ranges do not cover this project's independently configured SMTP
# mailer. Authentication uses Credentials; nodemailer powers the application's own
# email sender. Keep the same peer-resolution mode for lockfile installs in local work
# and CI, and check the complete dependency graph with npm audit.
legacy-peer-deps=true
